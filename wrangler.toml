name = "ero-sillytavern-pwa"
compatibility_date = "2024-01-01"

# PWA 静态站点 - 使用 assets 模式
[assets]
directory = "."
binding = "ASSETS"

# 排除不需要部署的目录
exclude = ["node_modules", "src", "tests", ".git", "dist"]